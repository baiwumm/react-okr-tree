# react-okr-tree 验收审计报告

> 本文档逐项核对 `docs/requirements.md` 的 §9 验收清单、§3 移植决策 R1–R9、§8 有意差异 D1–D12、
> §2.3 继承结论 Q1–Q9，每一项必须指向**可执行的证据**：测试文件（含用例名或条数）、
> `package.json` 里真实存在的脚本、`.github/workflows/` 里的 job，或构建产物断言脚本。
> 找不到证据的一律记 `❌ 未验证`，并写明补验证需要什么。
>
> 审计日期：2026-09-20。仓库状态：`packages/react-okr-tree` v0.1.0，`dist/` 与 `apps/website/out/`
> 为当日构建产物。本报告只做核对与记录，未修改任何实现代码。

## 1. 一句话结论

**代码与门禁本身已达发布条件（252 条单测、覆盖率四项、size-limit 三条、`verify:dist` 34 条、publint + attw 四格、`verify:export` 16 条全绿，Playwright 14 条用例逐条跑通过——唯一被判失败的那条是 trace 收尾撞上并行进程清目录，见第 8 节），但 §9 的 15 条里有 11 条只能给 ⚠️。**

> **2026-09-21 复验 + 同日第二批收口**：原先点名的三处「文档明写有、实际查无」**已全部清零**——`public/og.png` 与 `out/og.png` 补齐（第 15 条仅剩「`verify-export` 的死链正则扫不到 `og:image` meta」这个检测盲区，记为 G11 半收口）、Linux 视觉基线补 15 张（第 14 条，`d8aef8c`）、UMD 的 `<script>` 路径改为真实执行验证（第 13 条 / G10，恒真断言已删）。同时「CI 各 job 没有一次历史 run 可查」也不再成立：`ci.yml` / `visual.yml` 均已多次 success，`release.yml` 也跑过一次 success（run `35579145408`，publish 步被守卫跳过、只补建 GitHub Release）。原先「等你点头」的两件里，`version` 与 tag 不一致已随 1.13.0 发布消解，只剩 DOM 跨实现比对是否本期做。逐条现状见第 10 节，本节以下正文按初版原样保留。

除此之外全部是「功能在、断言缺」类缺口，共 17 条、按代价排在第 7 节，前 7 条是一行到一条断言的量级；第 5 条「D1–D10 之外差异为零」目前唯一的系统性盲区是第 6 条的 DOM 跨实现比对从未做过。

各表状态计数：

| 表                   | ✅ 已验证                   | ⚠️ 部分                                        | ❌ 未验证                           |
| -------------------- | --------------------------- | ---------------------------------------------- | ----------------------------------- |
| §9 验收项 1–15       | 4（第 2 / 4 / 11 / 12 条）  | 11                                             | 0（但第 15 条内含三个不成立的子项） |
| R1–R9                | 5（R2 / R4 / R5 / R7 / R8） | 4（R1 / R3 / R6 / R9）                         | 0                                   |
| D1–D12               | 11                          | 1（D3 锁定态）                                 | 0                                   |
| Q1–Q9 + 冻结 + 1.6.0 | 8                           | 2（Q6 六组动画的门禁只钉一组、Q9 的 UMD 路径） | 0                                   |

一句话补充：**没有出现「声称做了、其实没实现」的功能级假 ✅**——所有 ⚠️ 都是验收证据的缺口而非能力缺口；这一点由第 6 节的四个差集支撑（props / 事件 / 方法 / 导出面各 0 缺失）。唯一的反向问题是真的存在却没人钉：见第 7 节的 17 条「加断言」类缺口。

## 2. §9 验收项 1–15

| 条目                                                                                                                                           | 状态      | 证据（文件:行或命令）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 备注                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1（4.1–4.7 API 行为逐项一致：默认值 / 类型 / 抛错文案与时机 / 边界）                                                                           | ⚠️ 部分   | `tests/components/okr-tree.spec.tsx` 32 条（`:329` 缺 nodeKey 四个方法抛错且保留 `updateKeyChild` 截断拼写、`:346` `filter('')` 空值恢复、`:434` 内容优先级）；`tests/model/node.spec.ts:131` `showCollapsable=false` 强制全展开；`tests/components/controlled.spec.tsx:128/149/172` 受控双向与非受控；`tests/api-surface.spec.tsx` 3 条（方法面 ↔ `shared/api.ts` 双向差集）                                                                                                                                                                                                                         | 「传值但不传回调」的**受控锁定态**（R3 第 131 行、4.1 表）零用例——`controlled.spec.tsx:94` 的宿主始终成对传回调。另外 42 props / 14 回调没有运行时防漂移测试（该文件头注释自己承认 props/事件运行时不可枚举），默认值一致性目前只由 typecheck + 表格人工对照兜住                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2（2.3 继承结论 Q1–Q9 + 冻结数据 + 1.6.0 props 策略无回退）                                                                                    | ✅ 已验证 | 见第 5 节 Q 表，逐行给到文件:行；1.6.0「要么生效要么警告」= `tests/components/prop-sync.spec.tsx` 6 条                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 3（24 个 Demo 可交互、效果对齐、`.dark` ≠ `theme="auto"` 有用例与说明）                                                                        | ⚠️ 部分   | `apps/website/content/guide/demos.mdx`（24 个 `<DemoBlock file>`，24 个 `components/demo/*.tsx` 一一对应）；`out/docs/guide/demos/index.html` 内 276 个 `org-chart-node-label-inner`；`tests/visual/smoke.spec.ts`（整页水合零 console 报错）；`components/demo/theme-switcher.tsx:34`（6 内置 + 第 7 个「跟随站点主题」走 `resolvedTheme`）；`content/theme/index.mdx:40–43`（`.dark` 类 ≠ `prefers-color-scheme` 的说明与两条处置）；视觉同源：`app/globals.css` 内 33 处 `--color-fd-*` 覆盖 + `@custom-variant dark (&:is(.dark *))`（:5）+ `--radius: 0.625rem`（:71）+ oklch 黑白体系（:72 起） | 交互断言只覆盖 6 个用例（smoke）+ 10 张像素基线，其余 14 个只有「渲染/水合不报错」这一层；「效果与原 Demo 对齐」没有并排比对（与第 6 条同源）；`.dark` 态零基线（见第 15 条与 G12）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 4（三处内容定制 + 两处按钮定制优先级正确）                                                                                                     | ✅ 已验证 | `okr-tree.spec.tsx:434`（`renderNode > nodeComponent > renderContent`）、`a11y-group.spec.tsx:283`（同序第二条独立断言）、`controlled.spec.tsx:263`（`renderExpandBtn` 取代 `nodeBtnContent`）、`controlled.spec.tsx:294`（`showNodeNum` 优先于 `renderExpandBtn`）                                                                                                                                                                                                                                                                                                                                   | 四条优先级链全部有断言，顺序与 4.4 一致                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 5（D1–D10 之外的差异为零）                                                                                                                     | ⚠️ 部分   | 第 4 节 D 表 + 第 6 节差集（props / 事件 / 方法 / 导出面四项差集干净）+ `apps/website/content/migration/index.mdx`（D1–D12 成文）                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 这条本质是「不存在其它差异」，只能靠差集证否；DOM 层面缺跨实现比对（见第 6 条），所以严谨表述是「已知差异都落在 D1–D12 内」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 6（相同数据 + props 下 DOM 层级 / 标签 / 类名 / `role` / `aria-*` / `data-level` 与源项目一致）                                                | ⚠️ 部分   | `tests/components/dom-structure.spec.tsx` 4 条（三套布局结构快照 + 展开后内联样式）；`src/dom-contract.ts:82/90/93/99` 四条承重选择器与 Vue 逐字相同（对照 `vue3-okr-tree/src/lib/okr-tree/OkrTreeGroup.vue:54`、`OkrTree.vue:439/442/497`）                                                                                                                                                                                                                                                                                                                                                          | 快照锁的是**本实现自己的**形状。跨实现比对（同一份数据两边渲染再 diff）从未做过：dom-structure 文件头与 development-plan 4.7 都写「挪到 9.2」，但 9.2 的 Playwright 里没有起源项目取 DOM 的用例。补法：用 `page.route` 起 Vue playground 静态产物渲染同一份数据，把归一化结构字符串存进 `tests/visual/fixtures/` 做比对                                                                                                                                                                                                                                                                                                                                                                                  |
| 7（6 主题 / `unstyled` / `connector` 双模式 × 3 形状 / 6 组动画 / 打印 / 焦点环 视觉逐项对齐）                                                 | ⚠️ 部分   | `tests/visual/visual.spec.ts:78–86`（6 套主题各一张基线）、`:89`（`unstyled` 计算样式：阴影/边框/圆角/背景/内边距/盒尺寸 6 项）、`:133`（`emulateMedia print`）、`:51`（`connector="svg"`，仅 curve 一张）；CSS 侧 `verify:dist` 5 条 style.css 内容断言                                                                                                                                                                                                                                                                                                                                              | `orthogonal` / `straight` 两种形状**没有视觉位**，只有 `connector.spec.tsx` 的 `d` 字符串断言；6 组动画没有任何视觉回归位（`transition.css` 逐字节一致算间接证据，仓库内无常驻门禁）；焦点环 `--okr-focus-*` 只在 `content/theme/index.mdx:107` 与 `guide/keyboard.mdx:69` 成文，零断言                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 8（折叠子树保持挂载；展开/收起不改变 OKR 根节点水平坐标）                                                                                      | ⚠️ 部分   | 挂载半边 ✅：`query-methods.spec.tsx:70`、`dom-structure.spec.tsx:74`、`smoke.spec.ts:62–63`（`is-hidden` 后子节点计数仍 > 0）；坐标半边只有 `okr-tree.spec.tsx:92/101`（`align-root` 类的有无）+ 基线 `okr-group-aligned`                                                                                                                                                                                                                                                                                                                                                                            | 「不改变水平坐标」缺一条 `getBoundingClientRect().left/width` 的前后比对。补法：在 `visual.spec.ts` 里对 OKR 根卡片展开前后各量一次并 `toBeCloseTo`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 9（R1 局部更新：点击单节点与跨节点交互态只碰相关节点）                                                                                         | ⚠️ 部分   | `tests/spike-r1.spec.tsx` 6 条（点击单节点只重渲染该节点、跨节点交互态只重渲染新旧两个、一次事件多次赋值合并一次渲染、整树批量 = 每节点恰一次）；`tests/components/local-update.spec.tsx:37/65/100`                                                                                                                                                                                                                                                                                                                                                                                                   | 键盘漫游有渲染计数判据（`local-update:65`），**拖拽悬停（`dragOverNode` / `dragOverType`）没有**——R3 第 129 行与 9.3 第 9 条都点名了这一条，目前只有功能用例 `draggable.spec.tsx`（11 条），不含「只 bump 相关节点」判据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 10（模型层无框架 import；`style.css` / `transition.css` 与源项目 diff 仅限包名注释）                                                           | ⚠️ 部分   | 前半 ✅：`tests/model/framework-boundary.spec.ts` 2 条（`src/model/**` 不引用 `react`/`react-dom`；`viewport.ts` 只经变量动态 import `html-to-image`）；后半：本审计手工 `diff`（`transition.css` 0 行差异，`style.css` 仅 `@import` 一行 + 头部注释）                                                                                                                                                                                                                                                                                                                                                | CSS 一致性**没有仓库内可执行门禁**（Vue 仓库不是依赖，CI 也拿不到），只靠 review 纪律。建议把关键几何值（`--okr-gap-level:20px`、左树 stub 的 `12px` / `calc(100% - 11px)` / `10px` 三元组）写成常量断言钉进 `verify:dist`                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 11（性能门槛达标；`refreshData()` 与 `deepWatch:false` 各有单测）                                                                              | ✅ 已验证 | `data-refresh.spec.tsx:103`（`refreshData()`）/ `:118`（`deepWatch=false`）；`scripts/benchmark.mjs` 7 场景 → `docs/perf.md`；`tests/visual/perf.spec.ts` 2041 节点 Chromium 首渲染门禁，本审计实测 **36.5 ms / 2040 个渲染节点**（限额 300 ms，CI 1500 ms）                                                                                                                                                                                                                                                                                                                                          | `perf.spec.ts` 与 `tests/visual/global-setup.ts` 是本审计进行中（20:23–20:29）才落地的文件：第一次跑因夹具未构建失败，现由 `globalSetup` 自动打包；夹具另带「渲染节点数 > 2000」的有效性断言                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 12（SSR 冒烟覆盖清单；服务端不碰 `window`；无 hydration 警告）                                                                                 | ✅ 已验证 | `tests/ssr/render-to-string.spec.tsx` 6 条（`:25` 垂直 / `:36` 水平与 OKR 左树 / `:60` 受控 props / `:77` `renderNode`+`empty`+Group+Viewport / `:105` 不读 window 且无 console 噪音 / `:135` 未知主题在 effect 警告）；`verify:dist` 三格式首行 `'use client'` 断言；真实产物证据：`out/index.html` 与 `out/docs/guide/demos/index.html`（276 个节点标签，全部为服务端渲染）                                                                                                                                                                                                                         | hydration 是靠「整站在浏览器里水合零报错」（`smoke.spec.ts:108`）覆盖，没有单独的 `hydrateRoot` 用例——可接受，记录备查                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 13（三产物 + 类型 + CSS 齐备；ESM/CJS/UMD 三条引入路径；publint + attw 无错误；size-limit 达标）                                               | ⚠️ 部分   | 产物清单与两路径 ✅：`verify:dist` 45 条（`:160–170` `require()` 拿 `OkrTree`/`NODE_KEY`/`default === OkrTree`；`:173–186` 三格式 `'use client'`）；`pnpm verify:package` → publint `All good!` + attw 四格 🟢；`pnpm size` → ESM 18.65 / CSS 3.79 / UMD 16.84 kB 全过                                                                                                                                                                                                                                                                                                                                | **UMD `<script>` 这条路径没有任何可执行验证**：`scripts/verify-dist.mjs:189–190` 只把 UMD 当文本读，且 `/React/.test(umdSource)` 这个兜底分支让「UMD 外部化 react」几乎恒真（产物里必然含 `ReactOkrTree`），产物从未被真正加载执行。补法：`verify:dist` 里用 `vm`/`new Function` 在注入 React + ReactDOM 全局的上下文跑一遍 UMD，断言能挂载；顺带收紧那条恒真断言                                                                                                                                                                                                                                                                                                                                        |
| 14（≥226 条等价移植、覆盖率阈值达标、视觉回归与 CI 各 job 全绿）                                                                               | ⚠️ 部分   | 条数与阈值 ✅：252 条 / 92.96·85.52·94.96·95.71（阈值 80/75/80/80，见 `vite.config.ts:80`）；对位 ✅：本审计重数 Vue 侧逐文件 `it()` 恰好 226，其中 `components/perf` 的 5 条落在 `tests/model/node.spec.ts:272/296`（Q4 节点实例复用 `toBe` 断言）与 `data-refresh.spec.tsx`（deep-watch 开关），无行为丢失                                                                                                                                                                                                                                                                                          | 「CI 各 job 全绿」无法声称：四条 workflow（`ci.yml` 的 `verify`/`website`/`peer-matrix`、`visual.yml`、`snapshot-bootstrap.yml`、`release.yml`）**定义齐了但没有一次历史 run 可查**（本地 `git log` 未见 CI 徽章，仓库未推过 main）；且 **Linux 视觉基线一张都没有**（仓库内 `*-chromium-linux.png` = 0），`visual.yml` 跑在 ubuntu-24.04 上首轮必红。另两处与 §7 的 CI 行不一致：`peer-matrix` 实际钉的是 react `~18.2.0` + `~18.3.0`，而 §7 与 requirements 483 行写的是「React 18.2 / **19.2**」（19 由默认安装的 `verify` job 覆盖，但字面不符）；`website-deploy` job 已按计划 9.5 取消、改由 Cloudflare Workers Builds 跑 `npx wrangler deploy`（这一条是有意决定，`wrangler.jsonc` 在仓库根就位） |
| 15（`output:'export'` 通过；搜索索引生成且浏览器里搜得到；sitemap / robots / og 齐备；`wrangler deploy --dry-run`；亮色与 `.dark` 两态不串味） | ⚠️ 部分   | ✅ 部分：`node apps/website/scripts/verify-export.mjs` 16 条断言（无服务端残留 / 死链 0 / 索引结构可被 `staticClient` 载入 / sitemap 覆盖 16 个文档页 / 无 localhost 残留）、`wrangler.jsonc` 与 `next.config.mjs` 的 `output:'export'` 就位                                                                                                                                                                                                                                                                                                                                                          | ❌ 三件：① **og 齐备为假**——`app/layout.tsx:35` 写 `images: ['/og.png']`，但 `public/og.png` 与 `out/og.png` 都不存在，而 `verify-export` 的死链正则只扫 `href                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | src`，扫不到 `<meta property="og:image" content=…>`；② `wrangler deploy --dry-run` **没有任何脚本或 job 承载**（wrangler 有意不进 devDeps，本机也没装）；③「浏览器里搜得到结果」只有索引结构断言，没有一次真实查询（视觉套件里 0 处 search 用例），`.dark` 态也没有任何基线或计算样式断言 |

## 3. R1–R9 移植决策

| 条目                                                                                                                                                                                     | 状态      | 证据（文件:行或命令）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 备注                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R1 可变 store + `useSyncExternalStore`（局部更新、批量通知、render 阶段禁改）                                                                                                            | ⚠️ 部分   | `src/model/notifier.ts` + `src/hooks/use-node-version.ts`；`tests/spike-r1.spec.tsx`（6 条，含「一次事件内 4 次赋值合并为 1 次重渲染」「整树批量 = 每节点恰一次」）；`tests/components/local-update.spec.tsx:37`（只有被点节点版本前进）；`tests/model/node.spec.ts:60`（访问器等值短路：赋同值不通知）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 第 7 条硬约束「不得在 render 阶段调用会触发通知的方法」**没有任何执行机制**——既没有 ESLint 自定义规则（`eslint.config.js` 只接了 `typescript-eslint` + `react-hooks`），也没有 code-review 卡点，R1 原文设想的「setter 方法族 + lint」已按 2.4 的结论换成访问器，但 lint 那半没补                                                                            |
| R2 `data` 变更检测三档 + `refreshData()` 兜底                                                                                                                                            | ✅ 已验证 | `tests/components/data-refresh.spec.tsx` 5 条：`:49` 换引用重建、`:61` 数组换外壳元素同引用 ⇒ 不重建、`:78` 同引用原地变更靠渲染时脏检查、`:103` `refreshData()`、`:118` `deepWatch=false`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 四条路径 + 一条反例，比 R2 表格要求的多一条                                                                                                                                                                                                                                                                                                                  |
| R3 Vue 机制 → React 机制对照（三 Context 引用稳定、跨节点态只 bump 相关节点、`forwardRef`、受控成对 props、插槽→render props、插件/`createTypedOkrTree` 移除、`onNodeContextmenu` 判定） | ⚠️ 部分   | `src/context.ts`（三契约 + `configRef`）；`tests/components/prop-sync.spec.tsx:138`（换 `renderContent`/`renderNode` 立即重绘且**不重挂载**，钉住 configRef 设计）；`okr-tree.spec.tsx:307`（绑了回调才 `preventDefault`）；`okr-tree.spec.tsx:575` + `verify:dist` 的 `cjs.default === cjs.OkrTree`（D6）；`a11y-group.spec.tsx:398`（泛型只作用在类型层，D5）                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Context value 引用稳定是**间接**证明（局部更新用例若失效会红），没有一条「两次渲染之间 context value 同一」的直接断言；跨节点交互态里 **拖拽两态（`draggingNode` / `dragOverNode`）缺渲染计数**（同 §9 第 9 条）                                                                                                                                             |
| R4 组件划分（1:1 边界 + `svg-connector.ts` 独立成纯函数）                                                                                                                                | ✅ 已验证 | `packages/react-okr-tree/src/`：`index.ts` `types.ts` `context.ts` `cx.ts` `dom-contract.ts` `node-content.tsx` `svg-connector.ts` `viewport.ts` `OkrTree.tsx` `OkrTreeNode.tsx` `OkrTreeGroup.tsx` `OkrTreeViewport.tsx` `hooks/{use-node-version,use-reduced-motion,use-delayed-collapse}.ts` `model/{node,tree-store,util,notifier}.ts` `styles/{style,transition}.css`；`shared/api.ts` 落在包内                                                                                                                                                                                                                                                                                                                                                                                                                  | 与 4 节图一致（多出的 `cx.ts` / `dom-contract.ts` / `use-delayed-collapse.ts` 是落地期新增，见 development-plan 3.3 与 R7 第 2 点）                                                                                                                                                                                                                          |
| R5 事件回调签名（14 条，`node` 为内部 TreeNode，第三参移除）                                                                                                                             | ✅ 已验证 | `src/OkrTree.tsx:144–179` 14 个 `onXxx` 声明；`shared/api.ts` `eventsSection` 14 行与 Vue `eventsSection` 14 行逐项对应（见第 6 节差集）；`tests/components/interaction.spec.tsx` 9 条、`draggable.spec.tsx` 11 条（六个拖拽事件签名逐一断言）、`checkbox.spec.tsx` 13 条（`onCheck` 的 `TreeCheckInfo` 四字段、`onCheckChange` 每受影响节点各一次）                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | —                                                                                                                                                                                                                                                                                                                                                            |
| R6 DOM / 类名是契约（容器链 + 30 个状态类 + `is-current` 双处 + `div`）                                                                                                                  | ⚠️ 部分   | `src/dom-contract.ts:12/38`（`CLS` / `STATE` 常量表）；`tests/components/dom-structure.spec.tsx` 4 条快照；本审计逐个 `grep`：requirements 第 202 行列出的 **30 个状态类全部在 `src/` 出现**；`okr-tree.spec.tsx:241`（`is-current` 同时落卡片）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 状态类清单没有一条集合级断言（靠 grep 与快照间接覆盖）；`CARD_SELECTOR`（`dom-contract.ts:99`）**定义了但没有任何使用点**——`svg-connector.ts:69` 用 `CLS` 现场拼了同一条字符串，属死常量（轻微漂移，不影响行为）                                                                                                                                             |
| R7 样式零依赖 + 两套过渡机制（含 `animate` 关闭时同步卸载不依赖 rAF）                                                                                                                    | ✅ 已验证 | `packages/react-okr-tree/package.json` devDeps 内无 tailwind / css-in-js / headlessui / react-transition-group / dnd-kit，`html-to-image` 只在 devDeps + optional peer；`verify:dist` 4 条断言「es/cjs/umd 未静态引入可选 peer」+「ESM 保留 `@vite-ignore`/`webpackIgnore`/`turbopackIgnore`」；`tests/components/transition-robustness.spec.tsx` 2 条（rAF 节流环境下同步移除旧子节点；`animate` 开时 `is-animated` + `okr-anim-okr-fade-in` + `--okr-anim-duration: 50ms`，关时三者全无）；`src/hooks/use-delayed-collapse.ts`                                                                                                                                                                                                                                                                                      | CSS 与源项目的逐字节一致性见 §9 第 10 条（无仓库内门禁）                                                                                                                                                                                                                                                                                                     |
| R8 SSR / Next 兼容（顶层不碰 window、`getServerSnapshot`、effect 内注册、`'use client'` banner 带分号、受控初始值在创建期应用）                                                          | ✅ 已验证 | `tests/ssr/render-to-string.spec.tsx:105`（服务端不读 window、无 console 噪音）、`:60`（受控 props 服务端即生效 = 创建期应用的证据）、`:135`（警告在 effect 而非渲染期，StrictMode 双调用不重复）；`verify:dist:173–186` 断言三格式首行 `'use client'` 且以 `;`/换行收尾；真实证据：`pnpm build` 产物被文档站消费后 `out/index.html` 与 `out/docs/guide/demos/index.html` 内含 276 个服务端渲染节点标签                                                                                                                                                                                                                                                                                                                                                                                                               | —                                                                                                                                                                                                                                                                                                                                                            |
| R9 开发期警告 13 类 + 抛错文案与时机                                                                                                                                                     | ⚠️ 部分   | 抛错 ✅：`okr-tree.spec.tsx:329`（4 条 `[Tree] nodeKey is required in <方法>` + `[Tree] filterNodeMethod is required when filter`，含 `updateKeyChild` 截断拼写）、`okr-tree.spec.tsx:112`（`onlyBothTree` 缺 `leftData`）。警告：`controlled.spec.tsx:320`（`onlyBothTree` 非 horizontal + `leftData` 未开 onlyBothTree + `expanded-keys`/`current-key` 缺 nodeKey，四条文案）、`:338`（配置正确时零新增警告）、`prop-sync.spec.tsx:105`（三条「请为组件绑定 `key` 以重挂载」+ 去重）、`connector.spec.tsx:176`（非法 `connector` 回退 css）、`connector.spec.tsx:237`（非法 `connectorShape` 回退 curve）、`lazy-load.spec.tsx:233`（`lazy` 缺 `load` / `load` 缺 `lazy`）、`expand-methods.spec.ts:137`（重复 nodeKey）、`theme.spec.tsx:81`（自定义主题名）、`frozen-data.spec.tsx:103`（冻结数据回写跳过并警告） | 13 类里 11 类有文案级断言。两处没有：① `nodeKey` 缺失族的另外 3 条文案（`default-expanded-keys` / `default-checked-keys` / `current-node-key`——`src/OkrTree.tsx` 里会 emit，测试只覆盖到 `expanded-keys` 与 `current-key`）；② 前缀 `[react-okr-tree]` 本身（`src/model/util.ts:62` 实现，全套测试没有一条 `toContain('[react-okr-tree]')`）。功能在、门禁缺 |

## 4. D1–D12 有意差异

| 编号                                                                             | 状态      | 证据（文件:行或命令）                                                                                                                                                                                                                                                                                | 备注                                                                                                                                                                                            |
| -------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1 `renderContent` / `nodeBtnContent` 不再收 `h`                                 | ✅ 已验证 | `src/types.ts` 的 `RenderContentFunction` / `NodeBtnContentFunction` 单参签名；`tests/components/okr-tree.spec.tsx:400`（用例名直接写「React 版不传 h，D1」，断言入参是内部 `TreeNode`）、`:415`（`nodeBtnContent`）                                                                                 | —                                                                                                                                                                                               |
| D2 事件回调去掉第三参 `nodeComponent`                                            | ✅ 已验证 | `tests/components/okr-tree.spec.tsx:316–318`（`onNodeContextMenu` 实参解构恰为 `[event, data, node]` 三个）、`controlled.spec.tsx:145` 注释点名 D2；DOM 定位由 `:26` `getNodeEl` 承担（`query-methods`/`a11y-group` 用例使用）                                                                       | 类型层由 `src/OkrTree.tsx:144–179` + `pnpm typecheck` 兜住                                                                                                                                      |
| D3 `v-model:x` → `x` + `onXxxChange`，`undefined` 即非受控                       | ⚠️ 部分   | `tests/components/controlled.spec.tsx:128`（传值 ⇒ 按列表展开、点击回写回调）、`:172`（不传 ⇒ 不触发回调）、`:190`（`currentKey` 同构）；`OkrTreeViewport` 侧 `viewport.spec.tsx` 19 条含 `zoom` / `offset` 受控                                                                                     | **锁定态（传值但不传回调）零用例**——同 §9 第 1 条。判定「`undefined` = 非受控」的行为被覆盖了，「值在但回调缺席时内部不改状态」这条没有                                                         |
| D4 插槽 → render props，`children` 可作 `renderNode` 的函数形式                  | ✅ 已验证 | `okr-tree.spec.tsx:426`（`renderNode`）、`:555`（`children` 传函数等价 `renderNode`）、`:564`（`children` 传非函数不渲染并警告一次）、`:450`（`empty`）；`controlled.spec.tsx:263/280`（`renderExpandBtn` 的 `node`/`data`/`expanded`/`side`）；`viewport.spec.tsx`（`renderToolbar` 作用域五字段）  | 作用域参数形状与 4.4 表一致                                                                                                                                                                     |
| D5 移除 `createTypedOkrTree<T>`，泛型组件等价                                    | ✅ 已验证 | `a11y-group.spec.tsx:398`「`OkrTree<T>` 只作用于类型层：收窄 data 与回调参数，运行时渲染同一组件」；`tests-consumer/consumer.tsx` + `pnpm verify:peer-types`（产物 d.ts 上做泛型收窄编译验证）；`content/guide/typed.mdx` 改写为「泛型与类型推导」                                                   | 源项目导出的 `createTypedOkrTree` / `TypedOkrTreeSlots` 在 `src/index.ts` 已消失（第 6 节差集）                                                                                                 |
| D6 移除插件式全局注册，默认导出改为组件                                          | ✅ 已验证 | `okr-tree.spec.tsx:575`「默认导出与具名 OkrTree 同一引用（源码层面，D6）」；`verify:dist:167` `cjs.default === cjs.OkrTree`；`src/index.ts:53` `export { OkrTree as default }`                                                                                                                       | —                                                                                                                                                                                               |
| D7 原地变更降级为「渲染时脏检查 + `refreshData()`」                              | ✅ 已验证 | `data-refresh.spec.tsx` 5 条（`:61` 同引用不重建、`:78` 脏检查接住、`:103` `refreshData()`、`:118` `deepWatch=false`）；`src/OkrTree.tsx` 的 `store.isStructureDirty()` 只读脏检查；README 的「数据变更检测」独立成节                                                                                | 降级与新增 API 两侧都有测试与文档，R2 的三格表格逐格可指到用例                                                                                                                                  |
| D8 子容器挂载/卸载过渡允许简化，但保留两个语义                                   | ✅ 已验证 | `src/hooks/use-delayed-collapse.ts`（与源项目同构的延迟卸载）；`tests/components/transition-robustness.spec.tsx:69`（`animate` 关闭时同步卸载、不依赖 rAF，含「不残留 leave-active」断言）、`:122`（`animate` 开关与内联时长）；`dom-structure.spec.tsx:74`（展开后去掉 `visibility` 内联）          | 未引入 `react-transition-group`（devDeps 已核）                                                                                                                                                 |
| D9 `nodeKey` / `direction` / `onlyBothTree` 运行时变更不生效，警告提示绑定 `key` | ✅ 已验证 | `prop-sync.spec.tsx:105`：三条各警告**恰一次**（`messages.filter(...).length === 1`）且 `messages.every(m => m.includes('key 以重挂载'))`                                                                                                                                                            | 去重集合 + `resetWarnings()` 钩子在这条里被顺带钉住                                                                                                                                             |
| D10 类名前缀 / 包名 / 错误前缀改为 `react-okr-tree`，CSS 类名不变                | ✅   | CSS 类名不变 ✅：`verify:dist` 的 5 条 style.css 断言（`.org-chart-container`、`.okr-theme-{5}`、`prefers-color-scheme`、`var(--okr-line-color`、无全局 `*` reset）+ 本审计 `diff` 出 `transition.css` 0 行差异；前缀实现 ✅：`src/model/util.ts:62` `console.warn(\`[react-okr-tree] ${message}\`)` | 前缀**断言已补**（G3 同一条）：`tests/components/theme.spec.tsx:86` 断 `expect.stringContaining('[react-okr-tree]')`，另有 `controlled.spec.tsx:332`、`frozen-data.spec.tsx:103` 匹配消息正文 |
| D11 `event` 参数是 React 合成事件，原生事件取 `nativeEvent`                      | ✅ 已验证 | `okr-tree.spec.tsx:317` `expect(event.nativeEvent).toBe(evt)`（原生 `MouseEvent` 与回调里的 `nativeEvent` 同一实例）+ `:315` `evt.defaultPrevented === true`（合成事件转调原生的语义）                                                                                                               | 六个拖拽回调侧：`draggable.spec.tsx` 11 条按 React 合成事件形状断言（jsdom 无 `DragEvent`，见 development-plan 5.3 的替代构造）                                                                 |
| D12 定制回调返回 `ReactNode`                                                     | ✅ 已验证 | `src/types.ts` 的 `RenderContentFunction` 返回 `ReactNode`；`okr-tree.spec.tsx:400/415/426` 三条都直接返回 JSX 并由 DOM 断言生效                                                                                                                                                                     | 同 D1 的表现形式，无独立门禁需求                                                                                                                                                                |

## 5. Q1–Q9 继承结论（requirements 2.3）

| 条目                                                                             | 状态      | 证据（文件:行或命令）                                                                                                                                                                                                                                                                                                                                     | 备注                                                                                                                                                                                                                                 |
| -------------------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Q1 过滤从全部根遍历，父随可见后代保持可见                                        | ✅ 已验证 | `tests/model/tree-store.spec.ts:66` `describe('filter（Q1：全树过滤）')` 下 `:67`（父有可见后代不隐藏）、`:95`（第一层也参与过滤）、`:101`（多根时第二根的子树同样被过滤）、`:88`（空值恢复）；组件侧 `okr-tree.spec.tsx:346/363`（含 OKR 左右同时命中）、`query-methods.spec.tsx:86`                                                                     | —                                                                                                                                                                                                                                    |
| Q2 左右分表，同 key 双侧生效，`getNode` 右树优先                                 | ✅ 已验证 | `tree-store.spec.ts:162`（分表、同 key 不互相覆盖、右树优先）、`:171`（`defaultExpandedKeys` 同时生效）、`:222`（`currentNodeKey` 左右同时 + 置 null 全清）；`checkbox.spec.tsx:322`（勾选按左右独立维护，`setCheckedKeys` / `getCheckedKeys` 按 key 合并去重）；`model/lazy-load.spec.ts` 左树独立加载                                                   | —                                                                                                                                                                                                                                    |
| Q3 增删改同步回写源数据（正式行为）                                              | ✅ 已验证 | `tests/model/node.spec.ts:181` `describe('增删改（Q3：同步修改源数据，复刻原版行为）')` 8 条（`append` 写 `children`、`append` 省 parent 挂根、`insertBefore/After`、`remove` 注销 key、未设 nodeKey 时 `remove` 静默无效）；`okr-tree.spec.tsx:499`（方法驱动视图更新）；README「需要注意的行为」首条「会同步修改你传入的源数据（Q3）」                  | requirements 要求「作为正式行为写进 README」这一条也在                                                                                                                                                                               |
| Q4 原地变更增量重建并复用节点级状态                                              | ✅ 已验证 | `node.spec.ts:260` `describe('setData / updateChildren（Q4 与异步改 data）')`：`:272` 原地 push 后 `expect(store.getNode(1)).toBe(a)` 且 `a.expanded` 保留、`:286` 原地 splice 移除并注销、`:296` 同 key 换引用 ⇒ 复用实例 + 换绑 data + `expanded` 保留；`data-refresh.spec.tsx:78` 组件侧同语义                                                         | 覆盖源项目 `components/perf` 里「实例复用」的判据（第 6 节附录 B 对位说明）                                                                                                                                                          |
| Q5 / Q8 `animate` 三 prop 真实生效 + 折叠子树保持挂载占位                        | ✅ 已验证 | 生效：`transition-robustness.spec.tsx:122`（`is-animated` + `okr-anim-okr-fade-in` + `--okr-anim-duration: 50ms`，关闭时三者全无）、`reduced-motion.spec.tsx:65/71/81`（减弱动效时按关闭处理）；挂载占位：`query-methods.spec.tsx:70`（折叠子树不计入可见集但仍在 DOM）、`smoke.spec.ts:62–63`（真实浏览器里 `is-hidden` 后子节点计数仍 > 0）             | 「根节点不位移」的几何侧见 §9 第 8 条（只有类名 + 像素基线，无坐标断言）                                                                                                                                                             |
| Q6 六组 `okr-*` 动画全部迁移                                                     | ⚠️ 部分   | `src/styles/transition.css` 内六组 `okr-{fade-in-linear,fade-in,zoom-in-center,top,bottom,left}-enter/leave-*` 类全在（本审计枚举）+ `src/types.ts:14` `AnimateName` 六值 + `content/guide/demos.mdx` 动画用例的六个按钮；`verify:dist:135` 只钉了 `.okr-zoom-in-center-enter-active` 一组                                                                | 六组里只有一组被常驻门禁断言，其余五组的「迁到了」目前依赖 CSS 与源项目逐字一致这条**没有仓库内门禁**的事实（§9 第 10 条）。补法：`verify:dist` 里对六个名字逐一 `css.includes('.okr-' + name.replace('okr-','') + '-enter-active')` |
| Q7 九项死代码不移植                                                              | ✅ 已验证 | 本审计对 `selectedKey` / `orkstyle` / `props.leftChildren` / `findNearestComponent` / `updateLeftLeafState` / `computNodeStyle` / `ondeClass` / `okrEventBus` 逐个 `grep src/`：**零命中**（`leftChildren` 的命中是 `.org-chart-node-left-children` 容器与 `CLS.leftChildren` 类名常量，不是死 prop）；API 表差集里也不存在（第 6 节）                    | —                                                                                                                                                                                                                                    |
| Q9 `"type": "module"` 下额外产出 `.cjs`，三条引入路径可用                        | ⚠️ 部分   | `package.json:37` `main: ./dist/react-okr-tree.cjs` + `exports` 的 `require` 分支；`verify:dist:160–170` 真跑 `require()` 拿到 `OkrTree` / `NODE_KEY` / `default`；`pnpm verify:package` 的 attw `node16 (from CJS) 🟢 (CJS)` / `node16 (from ESM) 🟢 (ESM)`                                                                                              | 「三条路径都可用」里的 **`<script>` UMD 一条没有可执行验证**（同 §9 第 13 条）：`verify:dist` 只把 UMD 当文本读，从未加载执行                                                                                                        |
| 冻结 / 只读源数据：`defineProperty` 降级 WeakMap、回写类跳过并警告，不抛错不静默 | ✅ 已验证 | `tests/components/frozen-data.spec.tsx` 5 条：`:42` 浅冻结正常渲染与展开、`:60` 深冻结（`children` 也冻结）不抛异常、`:87` `append`/`remove` 收到警告且不抛错（`:103` 断言「冻结/只读源数据」文案）、`:115` `markNodeData` 失败降级 WeakMap、`:126` 未配 nodeKey 的冻结数据用 WeakMap 兜底 id 作列表 key                                                  | —                                                                                                                                                                                                                                    |
| 1.6.0 props 策略：要么生效、要么警告，不存在静默失效                             | ✅ 已验证 | `tests/components/prop-sync.spec.tsx` 6 条：`:33`（`showCollapsable` 即时显隐）、`:44`（`props` 字段映射运行时生效，`children` 变更触发重建）、`:65`（`leftData` 变更后按受控值恢复左树）、`:105`（三个创建期 prop 变更给警告）、`:138` / `:165`（渲染定制类与卡片尺寸组 prop 运行时生效且不重挂载——development-plan 4.3 记录的第三个缺口，由这两条钉住） | —                                                                                                                                                                                                                                    |

## 6. 特性对齐核对（Vue 清单 ↔ `shared/api.ts` + `src/OkrTree.tsx` 差集）

比对方法：把两侧 `shared/api.ts` 各分表第一列抽出来做双向差集（Vue 的 kebab-case 先按 4.1 的规则机械转成 camelCase 再比），
再用 `node -e "console.log(Object.keys(require('./dist/react-okr-tree.cjs')))"` 拿真实产物导出面与 `vue3-okr-tree/src/lib/index.ts` 比。
Vue 侧的方法清单取 `OkrTree.vue:1077` 的 `defineExpose`、props 取 `:95` 的 `defineProps`、事件取 `:249` 的 `defineEmits`。

### 6.1 Attributes

| 对比                     | 结果                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 行数                     | Vue 表 40 行 / React 表 42 行；Vue `defineProps` 实际 40 个 prop，与它的表一致                                                                                                                                                                                                                                                                                                                                        |
| 缺失（Vue 有、React 无） | **0**。40 个 prop 逐个在 `src/OkrTree.tsx:61–141` 出现，含两处原版拼写 `currentLableClassName`、`showCollapsable`（11.2）                                                                                                                                                                                                                                                                                             |
| 多余（React 有、Vue 无） | ① `onExpandedKeysChange` / `onCurrentKeyChange`——Vue 侧以 `update:expandedKeys` / `update:currentKey` 事件形式存在并被算进 events 表，React 按 D3 并入 attribute 行（表里写成 `expandedKeys + onExpandedKeysChange` 一行），**不是新增能力**；② `className` / `style`——requirements 4.1 明写的「新增 React 侧透传到 `.org-chart-container`」，**有意新增**；③ `children`（函数形式 = `renderNode`，D4），**有意新增** |
| 结论                     | 无漂移。计数差异 100% 由 D3/D4 + 4.1 明写的两项新增解释                                                                                                                                                                                                                                                                                                                                                               |

### 6.2 事件 / 回调

| 对比        | 结果                                                                                                                                                        |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 行数        | 两侧各 14 行，一一对应（`node-click`→`onNodeClick` … `node-drop`→`onNodeDrop`）                                                                             |
| 缺失 / 多余 | **均为 0**                                                                                                                                                  |
| 签名差异    | Vue 的前三个事件带第四位 `nodeComponent`（`OkrTree.vue:250–252`），React 去掉（D2）；`event` 类型由原生 `MouseEvent`/`DragEvent` 改为 React 合成事件（D11） |

### 6.3 Methods（`OkrTreeHandle` ↔ `defineExpose`）

| 对比                                               | 结果                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vue `defineExpose`                                 | `store` + `root` + 27 个方法（`filter` `getNodeKey` `setCurrentNode` `getNode` `getNodeEl` `setCurrentKey` `remove` `getCurrentNode` `getCurrentKey` `append` `insertBefore` `insertAfter` `updateKeyChildren` `expandAll` `collapseAll` `expandNode` `collapseNode` `scrollToNode` `getCheckedNodes` `getCheckedKeys` `getHalfCheckedNodes` `getHalfCheckedKeys` `setCheckedKeys` `isChecked` `moveNode` `getVisibleNodes` `getNodePath`） |
| React `OkrTreeHandle`（`src/OkrTree.tsx:194–232`） | 同样 `store` + `root` + 27 个 + `refreshData` = 30 个成员；`shared/api.ts` 的 `methodsSection` 29 行（`store / root` 合写一行）                                                                                                                                                                                                                                                                                                             |
| 缺失                                               | **0**，且有常驻门禁：`tests/api-surface.spec.tsx` 双向差集（表里每个方法在 ref 上存在、ref 上每个成员都进了表、行数恰为 29）                                                                                                                                                                                                                                                                                                                |
| 多余                                               | ① `refreshData`（D7/R2，有意新增）；② React 的表把 `getNodeKey` 写进了 Methods 而 **Vue 的表漏了这一行**（Vue `defineExpose` 有它、`methodsSection` 只有 26 行）——React 侧更完整，属**文档补全**而非漂移；③ 表里多一行 `store / root`，Vue 也公开了这两个成员但没进表，同为补全                                                                                                                                                             |
| 组合件                                             | `OkrTreeGroup` handle：Vue `defineExpose({refresh})` = React `{refresh}`（1:1）。`OkrTreeViewport` handle：Vue `defineExpose` 8 个（`zoomIn` `zoomOut` `reset` `fitToScreen` `centerNode` `exportImage` `getZoom` `getOffset`）与 `src/OkrTreeViewport.tsx:62–70` 逐字同名同序，1:1                                                                                                                                                         |

### 6.4 渲染定制表与组合件表

| 对比             | 结果                                                                                                                                                                                                                                                                                                                                |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Slots 表         | Vue 3 行（`default` / `expand-btn` / `empty`）→ React 4 行（`renderNode`（或 `children`） / `renderExpandBtn` / `empty` / `renderToolbar`）。前三行是 D4 的机械转换；`renderToolbar` 是 **Vue 有 `#toolbar` 插槽但没写进它的表**，React 补进表里——文档补全                                                                          |
| Group / 组合件表 | Vue `groupSection` 4 行（`align` / `default` slot / `refresh()` / 键盘导航）→ React `compositionSection` 7 行。多出的 3 行是 `OkrTreeViewport` 的 props / methods / `exportImage(options)`：**Vue 的 `shared/api.ts` 完全没有 OkrTreeViewport 这一张表**（全文只在 `getNodeEl` 的说明里提过一次），React 侧补齐。键盘导航行两侧同文 |
| 结论             | 六张表结构 1:1，React 三处「多出来」的内容全部是对 Vue 文档缺位的补全，无一项是行为漂移                                                                                                                                                                                                                                             |

### 6.5 导出面（真实产物 ↔ `vue3-okr-tree/src/lib/index.ts`）

| 类别                               | 清单                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 值导出（React 产物实测 19 个）     | `BUILT_IN_THEMES` `DEFAULT_PROPS` `NODE_KEY` `OkrTree` `OkrTreeGroup` `OkrTreeViewport` `TreeNode` `TreeStore` `clampZoom` `computeFit` `createNode` `default` `getNodeKey` `loadHtmlToImage` `markNodeData` `renderToDataUrl` `resetNodeIdSeed` `resetWarnings` `warn`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 缺失（Vue 有、React 无）           | `VueOkrTree`（别名）、`createTypedOkrTree`、`VueOkrTreePlugin` 与 `default = 插件` —— 三项分别是 D6 / D5 / D6 的移除项，**且 default 语义已改指组件**（`okr-tree.spec.tsx:575` + `verify:dist:167` 双向钉住）。另三类 `*Instance` 类型是 Vue 专有概念，React 侧由 `OkrTreeHandle` / `OkrTreeGroupHandle` / `OkrTreeViewportHandle` 取代                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 多余（React 有、Vue 的包入口没有） | ① `warn` / `resetWarnings`：requirements §7 单测行明写「`resetWarnings()` 测试钩子保留」，**有意**；② `BUILT_IN_THEMES`：requirements 4.7 把它列进「对齐 `src/lib/index.ts`」的清单，但 **Vue 的 `src/lib/index.ts` 其实没有导出它**（只在 `src/types/index.ts:24` 内部使用）——这是 requirements 4.7 与 Vue 事实的一处不一致，React 按 4.7 落了，建议回写 requirements；③ `DEFAULT_PROPS` / `resetNodeIdSeed` / 类型 `Subscribable` / `ViewportTreeApi` / `NodeScope` / `NodeComponent` / `NodeComponentProps` / `OkrTreeProps` / `TreeStoreOptions`：4.7 未列，属实现期新增。其中 `resetNodeIdSeed` 是**测试专用的播种重置**、`DEFAULT_PROPS` 是内部默认值表，两者被 public 出去会扩面（`NodeScope` 是 development-plan 4.3 记录的真实补缺，消费者标 `renderNode` 参数类型必需）。建议发布前把 `resetNodeIdSeed` 与 `DEFAULT_PROPS` 收进 `__internal` 命名空间或在 4.7 表里补登记，避免 1.x 想撤就是 breaking change |
| 类型层核对                         | `dist/index.d.ts` 里公开面上只出现 `ReactNode` / `useSyncExternalStore` 与项目自己的 `useEvent`，无 React 19 独有类型；由 `pnpm verify:peer-types`（`tsc -p tests-consumer/tsconfig.json`，`paths` 指到产物 d.ts）钉成门禁，本审计实跑通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

### 6.6 状态类与承重选择器

| 项                                           | 结果                                                                                                                                                                                                                                                                                            |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| requirements 第 202 行的 30 个状态/契约类    | 逐个 `grep src/`：全部存在（含「故意无样式」的 `org-chart-empty` 与 `okr-unstyled`）；但**没有一条集合级断言**，只有三套布局的结构快照会顺带覆盖其中一部分                                                                                                                                      |
| 三处依赖类名字符串的 JS 逻辑（R6 第 1–3 点） | 四条选择器与 Vue 逐字相同：`dom-contract.ts:82` ↔ `OkrTreeGroup.vue:54`、`:90` ↔ `OkrTree.vue:439`、`:93` ↔ `OkrTree.vue:442`、`:99` ↔ `OkrTree.vue:497`。`CARD_SELECTOR` 是**死常量**（`svg-connector.ts:69` 现场用 `CLS` 拼了同一条串），建议改成引用它，否则「契约」这句话在这一条上并不成立 |
| 组件内联几何                                 | 折叠指示短线 10/20 px（`svg-connector.ts:55` `const len = side === 'bottom' ? 20 : 10`）与 5.1 表一致                                                                                                                                                                                           |

## 7. 已知缺口与建议顺序

按修复代价从小到大排。前 7 条都是「一条断言 / 一个字符」量级，建议一次提交打包做完；
8–12 需要新用例或新脚本；13–15 需要外部条件（另一台机器、源项目产物、你的决策）。

> 本节保留初版原样。**2026-09-21 复验后的逐条现状见第 10 节**：G7 / G16 已收口、G11 半收口，其余仍成立。

| 顺序 | 缺口                                                                                                                                                                                     | 影响条目                  | 代价                               | 建议做法                                                                                                                                                                                                                                                                                                             |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ---------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1   | `apps/website/package.json:34` 的 `@types/mdx` 写成 `^2.0.14`，违反 §7 版本锁定行与附录 A「全部精确版本、不用 `^`/`~`」（全仓只有这一处违规，`html-to-image: ^1.11.0` 是 peer 范围不算） | §7 / 附录 A               | 一行                               | 改成 `2.0.14` 并更新 lockfile                                                                                                                                                                                                                                                                                        |
| G2   | `CARD_SELECTOR`（`src/dom-contract.ts:99`）定义了却没人用，`svg-connector.ts:69` 现场用 `CLS` 拼了同一条串                                                                               | R6                        | 一行                               | 改成引用 `CARD_SELECTOR`，否则「选择器是契约」这一条没有单一来源                                                                                                                                                                                                                                                     |
| G3   | 警告前缀 `[react-okr-tree]` 零断言（只存在于 `src/model/util.ts:62`）                                                                                                                    | D10 / R9                  | 一条 `expect`                      | 在 `controlled.spec.tsx:320` 那条里加 `messages.some(m => m.startsWith('[react-okr-tree]'))`                                                                                                                                                                                                                         |
| G4   | `nodeKey` 缺失警告族只断言了 `expanded-keys` / `current-key` 两条，缺 `default-expanded-keys` / `default-checked-keys` / `current-node-key` 三条                                         | R9 / §9 第 1 条           | 一个用例                           | 在 `controlled.spec.tsx` 补一棵只传这三个 prop、不传 `nodeKey` 的树，按文案逐条断言                                                                                                                                                                                                                                  |
| G5   | 六组 `okr-*` 动画只有一组进常驻门禁（`verify:dist:135` 写死 `.okr-zoom-in-center-enter-active`）                                                                                         | Q6 / §9 第 7 条           | 一条循环                           | `verify:dist` 改为对 `AnimateName` 六个值逐一 `css.includes(...)`                                                                                                                                                                                                                                                    |
| G6   | requirements 4.7 声称 `BUILT_IN_THEMES` 「对齐 `src/lib/index.ts`」，但 Vue 的入口并未导出它（只在其 `src/types/index.ts:24` 内部使用）                                                  | §6 差集 / §4.7            | 文档回写                           | 在 4.7 注一句「React 侧额外公开」，或按 6.5 的建议把 `resetNodeIdSeed` / `DEFAULT_PROPS` 一并登记，避免 1.x 回收成 breaking change                                                                                                                                                                                   |
| G7   | `release.yml` 会比对 `package.json` 的 `version` 与 tag，而当前是 `0.1.0`、`CHANGELOG.md` 已写 `1.0.0`                                                                                   | §9 第 14 条 / 9.9         | 一行 + 你的批准                    | 发布提交里把 version 改 `1.0.0` 并打 `v1.0.0`；**这是开发计划 9.9 明写「等你点头」的那一步，不是缺陷**                                                                                                                                                                                                               |
| G8   | 受控「锁定态」（传值但不传回调）零用例                                                                                                                                                   | §9 第 1 条 / D3 / R3      | 一个新用例                         | `controlled.spec.tsx` 加一条：只传 `expandedKeys`，点 `+/-` 后断言视图不变、回调不触发                                                                                                                                                                                                                               |
| G9   | 拖拽悬停的跨节点状态更新没有渲染计数判据（R3 第 129 行点名、§9 第 9 条要求）                                                                                                             | §9 第 9 条 / R1 / R3      | 一个新用例                         | 照 `local-update.spec.tsx:65` 的形状写一条 `dragEnter/dragOver` 版本，断言只有 `dragOverNode` 新旧两个 bump；顺带把 `dragging` 一并覆盖                                                                                                                                                                              |
| G10  | UMD 的 `<script>` 引入路径**从未被执行**，且 `verify-dist.mjs:190` 的 `\|\| /React/.test(umdSource)` 使「UMD 外部化 react」近乎恒真                                                      | §9 第 13 条 / Q9          | 中（新脚本段）                     | `verify:dist` 里用 `node:vm` 在全局注入 `React`/`ReactDOM` 后跑 UMD，断言 `window.ReactOkrTree.OkrTree` 可挂载；同时删掉那条兜底分支，只留 `/factory\(exports, ?React\)/` 形状判定                                                                                                                                   |
| G11  | `public/og.png` 不存在，但 `app/layout.tsx:35` 声明了 `images: ['/og.png']`；`verify-export` 的死链正则只扫 `href\|src`，扫不到 `<meta … content>`                                       | §9 第 15 条               | 中（一张图 + 一条断言）            | 补静态 `public/og.png`（开发计划 7.2 已把这步挂到设计资源上），并在 `verify-export` 的资产清单里加 `og.png` + 把 `property="og:image"` 的 `content` 纳入死链扫描                                                                                                                                                     |
| G12  | 「浏览器里搜得到结果」只有索引结构断言（`verify-export.mjs:87`），没有一次真实查询；`.dark` 态零基线零断言                                                                               | §9 第 15 条 / 6.3 第 3 点 | 中（2 条视觉用例）                 | `tests/visual/` 加一条：`page.goto` 后触发站内搜索浮层、输入一个只出现在某页的词、断言结果标题命中；再加一条 `emulateMedia({colorScheme:'dark'})` 或 `addInitScript` 写 `localStorage.theme='dark'` 后对 2 个 Demo 拍基线，证明确实不串味                                                                            |
| G13  | 焦点环与 `connector` 的 `orthogonal` / `straight` 两种形状在视觉层零覆盖                                                                                                                 | §9 第 7 条                | 中（3 张基线）                     | `visual.spec.ts` 的形状用例改成三种形状各一张；焦点环补一条 `keyboard.press('Tab')` 后的 `outline` 计算样式断言（不依赖像素）                                                                                                                                                                                        |
| G14  | 「展开/收起不改变 OKR 根节点水平坐标」没有几何断言，只有 `align-root` 类名与一张像素基线                                                                                                 | §9 第 8 条                | 中（1 条几何用例）                 | `visual.spec.ts` 里对 OKR 根卡片量 `getBoundingClientRect()` 的 `left`/`width`，展开→收起前后各一次 `toBeCloseTo(…, 0)`                                                                                                                                                                                              |
| G15  | DOM 与源项目的**跨实现比对**从未做过（快照锁的是本实现自己的形状；文件头与 4.7 都写着「挪到 9.2」，9.2 里却没有）                                                                        | §9 第 6 条                | 大（要起 Vue 产物 + 归一化 diff）  | 用 `page.route` 把 `vue3-okr-tree/playground` 的静态产物挂进 Playwright，渲染与 `dom-structure.spec.tsx` 同一份数据，导出同款归一化结构字符串存成 fixture，再与本侧字符串 diff。这是第 5 条（D1–D10 之外差异为零）目前唯一的系统性盲区                                                                               |
| G16  | Linux 视觉基线 **0 张**（`find *-chromium-linux.png` 为空），而 `visual.yml` 跑在 ubuntu-24.04：`updateSnapshots:'missing'` 仍判失败，首轮必红                                           | §9 第 14 条               | 大（要一次 CI 往返）               | 路径已经备好：Actions 手动跑 `snapshot-bootstrap.yml` → 下载 `linux-snapshots` artifact → 仓库根解包 → 提交 15 张 png。这一步只能联网跑一次并人工过一遍图                                                                                                                                                            |
| G17  | `style.css` / `transition.css` 与源项目「diff 仅限包名注释」没有任何常驻门禁（Vue 仓库不是依赖，CI 拿不到）                                                                              | §9 第 10 条               | 大（要么固化指纹要么 CI 拉源仓库） | 轻量做法：把关键几何值写成一条常量断言（`--okr-gap-level: 20px`、`--okr-gap-sibling: 5px`、左树 stub 的 `width:12px` / `left:calc(100% - 11px)` / `height:10px` 三元组、`-1px` 修正、`.okr-unstyled` 的 5 类选择器），放在 `verify:dist` 的 CSS 段里；重装做法：CI 里 `git clone --depth 1` 源仓库做一次 diff 白名单 |

## 8. 本次实跑的命令与结果

| 命令                                               | 工作目录                  | 结果                                                                                                                                                                        | 退出码 |
| -------------------------------------------------- | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `pnpm test`                                        | 仓库根                    | `Test Files 26 passed (26)` / `Tests 252 passed (252)`，4.44 s                                                                                                              | 0      |
| `pnpm typecheck`                                   | 仓库根                    | `tsc --noEmit -p tsconfig.json`，无输出                                                                                                                                     | 0      |
| `pnpm lint`                                        | 仓库根                    | `eslint .`，无输出（`react-hooks/exhaustive-deps` 未被关闭，见 `eslint.config.js`）                                                                                         | 0      |
| `pnpm verify:dist`                                 | 仓库根                    | 34 条产物断言逐条 `ok`，末行 `[verify:dist] ALL PASSED`                                                                                                                     | 0      |
| `pnpm verify:peer-types`                           | 仓库根                    | `tsc --noEmit -p tests-consumer/tsconfig.json`，无输出（`dist/index.d.ts` 在 `@types/react@19` 下可编译消费者示例）                                                         | 0      |
| `pnpm test:coverage`                               | 仓库根                    | 252 条全过；覆盖率 statements **92.96** / branches **85.52** / functions **94.96** / lines **95.71**，阈值 80/75/80/80 四项均达标                                           | 0      |
| `pnpm size`                                        | 仓库根                    | ESM **16.91 kB** / 20 kB（余 15.5%）、style.css **3.79 kB** / 4 kB（余 5.2%）、UMD **17.02 kB** / 21 kB。ESM 由 18.65 降下来是因为压缩器从 Oxc 换成 Terser（`format.comments` 白名单保住可选 peer 的三条 ignore 注释，见 `verify:dist`），产物不再是未压缩形态                     | 0      |
| `npx playwright test`                              | `packages/react-okr-tree` | 首跑（20:16，13 条时）`13 passed (37.4s)`；套件随后被并行作业扩到 **14 条**（新增 `perf.spec.ts`），隔离端口重跑（`OKR_VISUAL_PORT=4611`）得 `13 passed / 1 failed (40.9s)` | 0 / 1  |
| `node scripts/verify-export.mjs`                   | `apps/website`            | 16 条断言逐条 `ok`，末行 `[verify:export] ALL PASSED`（导出 HTML 20 个、死链 0、sitemap 覆盖 16 个文档页）                                                                  | 0      |
| `pnpm build`                                       | 仓库根                    | 三格式各一遍（es / umd / cjs）+ `[post-build] 已生成 dist/index.d.cts`，`✓ built in 1.86s`                                                                                  | 0      |
| `pnpm verify:package`（`publint` + `attw --pack`） | 仓库根                    | publint `All good!`；attw `No problems found`，node10 / node16 CJS / node16 ESM / bundler **四格全 🟢**                                                                     | 0      |
| CSS 与源项目对比（本审计临时执行，非仓库脚本）     | 仓库根                    | `diff` 后 `transition.css` **0 行差异**；`style.css` 仅差 `@import` 路径 1 行 + 头部注释段                                                                                  | —      |

关于 Playwright 那 1 条失败与端口争用（务必按此理解，别当成产物缺陷）：

- 唯一失败的 `visual.spec.ts:51 SVG 连接线`，**用例体是通过的**，报错是 `browserContext.close: ENOENT …/test-results/.playwright-artifacts-0/traces/…-recording16.trace`——
  审计期间同一工作树里有第二个 Playwright 进程在并行跑（`.github/workflows/release.yml`、`snapshot-bootstrap.yml`、`tests/visual/perf.spec.ts` 都是 20:23–20:29 之间新落地的文件），
  它清空 `test-results/` 时正好截走本进程正在收尾的 trace 分片。中途一次未隔离端口的跑批里，12 条用例直接 `ERR_CONNECTION_REFUSED`（`reuseExistingServer: !CI` 复用了对方正在退出的 4520 服务器），
  换 4611 后即恢复 13 通过。**结论：套件本身绿；本地并行跑视觉套件需要各自指定 `OKR_VISUAL_PORT`**（建议，见第 7 节 G9）。
- 性能门禁实测：`[perf] 2041 节点 Chromium 首渲染（5 轮取最优）: 36.5 ms / 2040 个节点`（限额 300 ms，CI 1500 ms）。

补充说明：

- 252 条测试按文件分布（数字取自 vitest 的逐文件输出）：`tests/spike-r1.spec.tsx` 6、
  `tests/api-surface.spec.tsx` 3、`tests/ssr/render-to-string.spec.tsx` 6、
  `tests/model/`：`node` 26 / `tree-store` 18 / `lazy-load` 10 / `expand-methods` 10 / `framework-boundary` 2、
  `tests/components/`：`okr-tree` 32 / `viewport` 19 / `a11y-group` 15 / `checkbox` 13 / `controlled` 12 /
  `connector` 12 / `draggable` 11 / `interaction` 9 / `lazy-load` 8 / `prop-sync` 6 / `query-methods` 6 /
  `theme` 6 / `data-refresh` 5 / `frozen-data` 5 / `dom-structure` 4 / `local-update` 3 /
  `reduced-motion` 3 / `transition-robustness` 2。合计 252。
- 视觉套件的 14 条 = `tests/visual/perf.spec.ts` 1 条（性能门禁）+ `smoke.spec.ts` 1 条（整页交互与零控制台报错）
  - `visual.spec.ts` 12 条（布局与几何 9 + 主题 1 条循环截 6 张 + 计算样式契约 2）。
    **像素基线只有 win32 一套（15 张 png）**，`*-chromium-linux.png` 一张都没有（本审计 `find` 结果为 0），详见 §9 第 14 条与第 7 节 G3。
- 本审计进行中（20:23–20:29）`tests/visual/perf.spec.ts`、`tests/visual/global-setup.ts`、
  `.github/workflows/release.yml`、`.github/workflows/snapshot-bootstrap.yml` 四个文件被并行作业新增，
  `development-plan.md` 的 9.2/9.4/9.5/9.7/9.8/9.9 同时被勾选。上表所有结论都是**这些文件到位之后**复跑得到的，
  但表里的行号若与最终提交有 ±1 行偏差，以文件内容为准。
- 未跑的命令：`pnpm build:website`（`out/` 已是当日 19:19 产物且 `verify:export` 16 条通过、视觉套件正跑在该产物上）、
  `pnpm bench`（jsdom 基线数值已在 `docs/perf.md`，浏览器侧门禁已由 `perf.spec.ts` 实跑）、
  `wrangler deploy --dry-run`（wrangler 有意不装进 devDeps，本机也没有；仓库里没有任何脚本或 job 承载这条命令）。

## 9. 报告之后的增量（同一日，全量门禁复跑）

审计 agent 到达轮次上限中断在第 7 节，以下是它交回之后补掉的与仍留着的：

| 项                                              | 变化                                                                                                                                                                | 证据                                                          |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- |
| 验收项 15 的「og 齐备」子项                     | 由 ❌ 转 ✅：`public/og.png`（1200×630）已生成，版式对齐源站 og-image.png（深色卡 + 品牌标记 + 标题 + 一行特性 + 域名），`metadata.openGraph.images` 生效           | `packages/react-okr-tree/scripts/gen-og.mjs`（`pnpm gen:og`） |
| 验收项 15 的「`wrangler deploy --dry-run`」子项 | 由 ❌ 转 ✅：从仓库根读到 `apps/website/out` 的 261 个文件并正常退出；wrangler 仍不进 devDependencies（与源项目一致，只在 Workers Builds 里 `npx wrangler deploy`） | `npx --yes wrangler@4.135.0 deploy --dry-run`                 |
| 品牌资产                                        | 换回 okr-tree 家族既有的一套标记（`favicon.svg` / `logo.svg` / `logo-dark.svg` / `apple-touch-icon.png`），此前文档站用的是临时手绘的树形标记                       | `apps/website/public/`                                        |
| 视觉门禁稳定性                                  | 之前观察到的「随机红 1–2 条」经复跑定位为**两个 Playwright 套件并发共用 `test-results/`**，不是像素不稳；单独连跑两次 14 条全绿                                     | `playwright.config.ts` 的注释里记了这条排查结论               |

仍然成立、需要人来做的两件事：

1. **浏览器里实搜那一次没做**（内置浏览器连不上本机端口，`curl` 同端口 200）。已做的是等价强度：用**同一个** `staticClient` 对导出的 `out/api/search` 走「取回 → 加载 → 检索」，命中与 `<mark>` 高亮正常。部署后建议在真浏览器里搜一次并核 `.dark` 两态是否串味（验收项 15 的最后一子项）。
2. **Linux 视觉基线**要在 GitHub Actions 手动跑一次 Snapshot Bootstrap 再提交（缺基线时 Playwright 必红，没有自动补齐这回事）。

### 全量门禁（2026-09-20 收工复跑，单机）

| 门禁                                       | 结果                                                                 |
| ------------------------------------------ | -------------------------------------------------------------------- |
| `format:check`（两包）                     | 全绿                                                                 |
| `lint` / `typecheck` / `verify:peer-types` | 全绿                                                                 |
| `test` / `test:coverage`                   | 252 条通过；覆盖率 92.96 / 85.52 / 94.96 / 95.71（阈值 80/75/80/80） |
| `build:check`（含 `verify:dist`）          | 全绿，34 条产物断言                                                  |
| `verify:package`（publint + attw）         | All good / 四格 🟢                                                   |
| `size`                                     | ESM 18.48 / CSS 3.79 / UMD 16.66 kB（gzip，预算 20/4/21）            |
| `bench`（jsdom 8 个场景）                  | 全绿，数值见 `docs/perf.md`                                          |
| `build:website` + `verify:export`          | 16 条文档路由、零死链、索引与资产齐备、sitemap 全覆盖                |
| `test:visual`（Playwright，单独连跑两次）  | 14 条全绿；含 Chromium 首渲染 36.9 ms / 2040 节点（门禁 300 ms）     |

## 10. 2026-09-21 逐条校准（第 7 节 G1–G17 现状）

校准方法：不读文档结论，直接对源码与产物重测（`grep` 判据落点 + `find` 数基线 + `gh run list` 查真实 run + 读 `verify-dist.mjs` / `verify-export.mjs` 断言体）。第 7 节的原表按初版保留，现状以下表为准。

| 缺口 | 现状      | 实测依据                                                                                                                                                                                                                                                                            |
| ---- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1   | 仍成立    | `apps/website/package.json:34` 仍是 `"@types/mdx": "^2.0.14"`                                                                                                                                                                                                                       |
| G2   | ✅ 已收口 | `svg-connector.ts:1` 已 `import { CARD_SELECTOR }`，`:70` 用它查询卡片锚点（`1d33a4d`），不再现场用 `CLS` 拼同一条串                                                                                                                                                                            |
| G3   | ✅ 已收口 | 同日补：`tests/components/theme.spec.tsx` 的未知 theme 警告用例现同时断言 `[react-okr-tree]` 前缀（上游 vue3 侧同批补了 `[vue3-okr-tree]`，此前两端都只断文案）                                                                                                                     |
| G4   | ✅ 已收口 | `controlled.spec.tsx` 新增一条：`default-expanded-keys` / `default-checked-keys` / `currentNodeKey` 三条各单独挂一次并断言对应警告，外加「补齐 node-key 后三条都不许出现」的反向守卫。两条踩坑记录：① `warn` 默认按文案去重，反向挂载前不 `resetWarnings()` 拿到的是空消息表，反向守卫变永真——实测把 `if (!p.nodeKey)` 改成无条件警告时该守卫照样绿、红的是文件里另一条老用例，现每次挂载前清表，改无条件后两条用例一起红；② 旧 pooled 断言里 `current-key` 那条文案本就写着「current-key / currentNodeKey」，只传 `currentNodeKey` 时靠子串也能命中，摘掉 `|| p.currentNodeKey !== undefined` 也不会红，故按「这一次挂载的消息集合」逐条断。三条变异（摘 default-expanded-keys 分支 / 摘 currentNodeKey 半个条件 / 去掉 node-key 守卫）两仓各跑一遍，全部只红新用例。上游 `vue3-okr-tree` 同批同形                                                                                                                                                                                                                                |
| G5   | ✅ 已收口 | 同日补：`transition-robustness.spec.tsx` 用 `it.each` 逐个断六种 `animateName` 的 `okr-anim-<name>` 类；`verify-dist.mjs` 的 CSS 侧由「只钉一组」改为逐个断 `enter-active` / `leave-active`                                                                                         |
| G6   | ✅ 已收口 | vue3 入口现已导出 `BUILT_IN_THEMES`（`vue3-okr-tree/src/lib/index.ts:13`，1.14.0 那批），本行原记的「实测导出 17 个、**不含** `BUILT_IN_THEMES`」已过时；requirements 4.7 的「对齐」表述到此成立                                                                                    |
| G7   | ✅ 已收口 | `packages/react-okr-tree/package.json` 现为 `1.13.0`，与 tag `v1.13.0` 一致；`release.yml` 的 tag 比对步在 run `35579145408` 实跑通过                                                                                                                                               |
| G8   | ✅ 已收口 | `controlled.spec.tsx` 新增两条锁定态用例，并**实测纠正了这条缺口原本的判据**：报告与 requirements 第 5 节点名的「传值但不传回调」并**不冻结视图** —— 展开态的渲染源是 store，受控 prop 只在创建期（`OkrTree.tsx:293`）与宿主传入值变化时（`:832-837`）回灌，宿主不回写就没有第二次同步，点击照常折叠（上游 vue3 侧同样三种写法实测一致，故这不是复刻偏差而是两仓共同语义；报告建议的「断视图不变」照写会得到一条红用例）。现两条用例钉现状：① 不传回调 → 不报错不警告、点击生效；② 传了回调但宿主不回写 → 回调照样收到新值（收起根节点即 `[]`）、视图不被拉回。三条变异验证：R1 摘掉 emit（红 ②）、R2 在交互回调里以 prop 为准拉回＝真冻结（**只红这两条**）、R3 去掉 node-key 守卫让锁定态也警告（红 ①）。**R3 之前 ① 的 `not.toHaveBeenCalled()` 是白断的**：`warn()` 按文案去重且去重表是模块级的，本文件前面的用例已经把那条文案发掉，不自己 `resetWarnings()` 就恒真（实测改无条件警告时它照样绿）。**仍未验**：`guide/controlled.mdx:91-93` 说「传内联字面量才完全锁定、引用稳定则保留」——探针没分出这个差别（宿主重渲染时 `data={makeData()}` 也换了引用 → 整树重建，两条路径混在一起），要证得先给宿主一份引用稳定的 data 再对比，已记待办。驱动 `_scratch/probe-locked-mutation.mjs`                                                                                                                                                                                                                                                |
| G9   | ✅ 已收口 | `local-update.spec.tsx` 新增一条拖拽判据，按事件顺序逐个断「谁被 bump」：dragstart 零 bump（`dragging` 只写在 ref 上，没有任何渲染状态读它）→ 首次悬停只 bump 被悬停的那个节点 → 同节点同分区重复 dragover 零 bump（`setDragOver` 的同值早退）→ 换目标恰好两个 bump（旧节点撤下 + 新节点挂上）→ dragEnter 零 bump（只转发事件）。同时断对应 label 上确有 `drop-inner` / `drop-prev` 类，避免「bump 数为 0 是因为指示根本没挂上」这种空断。五条变异逐个跑过、全部只红这条：摘同值早退 / 不 notify 旧节点 / 改成整树 notify / `setDraggingNode` 也 notify / dragEnter 顺手写 dragOver（驱动 `_scratch/probe-drag-mutation-react.mjs`，自带基线绿跑、锚点唯一性自检与 finally 还原）。**上游 vue3 侧同批补了同名文件的对称判据，但落点不同**：vue3 的拖拽指示是 provide 下来的 `shallowRef`，实测换一次目标时 18 个节点的 `labelWrapperClass` 全部重新求值、只有真变化的 2 个元素被写入 DOM，所以那边钉的是「DOM 写入落点」而不是版本 bump 数——机制差，不是复刻偏差。单测 281 → **282** 条 |
| G10  | ✅ 已收口 | 同日改：恒真的 `\|\| /React/.test(umdSource)` 分支已删，现断言 UMD 的 CJS 分支确实 `require('react')`（react 未被内联），并用 `new Function('module','exports','require', src)` **真实执行** UMD，断言 `default === OkrTree` 与 `BUILT_IN_THEMES` 6 项。`pnpm verify:dist` 实跑通过 |
| G11  | ◐ 半收口  | `public/og.png` 与 `out/og.png` 均已存在；但 `verify-export.mjs` 内 grep `og:image` / `property=` 零命中，「扫不到 og:image meta」这个检测盲区仍在                                                                                                                                  |
| G12  | 仍成立    | `tests/` 内 `search` 零命中、`.dark` 零命中                                                                                                                                                                                                                                         |
| G13  | 仍成立    | `tests/visual/` 内 `orthogonal` / `straight` / 焦点环零命中                                                                                                                                                                                                                         |
| G14  | ✅ 已收口 | `visual.spec.ts` 新增「展开 / 收起不改变 OKR 根卡片的水平坐标」：对 `#demo-okr-group` 第一棵树的根卡片量 `getBoundingClientRect()`，**取相对 `.org-chart-container` 的水平偏移**（绝对 `left` 会被点击时的自动横向滚动改掉，实测 962 → 591，那是滚动位置不是布局），右子树收起 → 重新展开 → 左子树收起 → 重新展开共四次 `toBeCloseTo(x, 0)` 与两次宽度比对，每步同时断 `.org-chart-node-children` / `-left-children` 上 `is-hidden` 的确切出现与消失，防「坐标不变只是因为按钮没生效」。**只加断言、没加任何截图基线**（png 数不变）。**四条变异**跑过，结论按实测写在这里：给 `.is-hidden` 追加一条 `display:none`（收起即从布局消失）→ 本条**红**、像素基线同样红；`flex:1 1 0` → `0 0 auto`、根节点 `width:100%` → `auto` 两条 → 本条与 `okr-group-aligned` 基线**都绿**，它们由 `group-align.spec.ts` 守着（实测两条都把它打红）；另有 `min-width:max-content` → `0` 一条由像素基线单独抓住（本条绿）。即这条守的是「收起这个动作不推根节点」，不是 `align-root` 的静态形状。变异改的是 `out/` 里那份打包 CSS（gitignored 产物，逐字还原），因为浏览器读的就是它，改 `src/styles` 要重跑两次构建。上游 `vue3-okr-tree` 同批同形（其 `demo-10`），四条门禁归属逐条复测一致。视觉用例 33 → **34** 条 |
| G15  | ✅ 已收口 | 两条常驻门禁共 15 个模式。**DOM**：`tests/visual/cross-impl.spec.ts` + `tests/visual/fixtures/cross-impl-vue3.json`（10 个非 svg 模式）——vue3 侧真实浏览器渲染的 outerHTML 固化成夹具，react 侧用 `dist/*.cjs` + `renderToStaticMarkup` 现算，两侧在同一个 Chromium 里各过一遍 DOM 往返后 diff，拆成 **structure**（剔 style 与 draggable）与 **geometry**（含 style，按 CSSOM 逐条声明取规范值）两条断言——原先设想的「含内联样式逐字一致」直接比属性串做不到。**SVG 连线 d 值**：`tests/visual/cross-impl-svg.spec.ts`（5 个 `connector="svg"` 模式，同一份夹具的 `paths` 字段）——d 是挂载后量布局算的，SSR 与 jsdom 都拿不到，故 react 侧也起真实浏览器（`fixtures/cross-impl-svg-entry.tsx` + `vite.cross-impl-svg.config.mjs` 打成自包含 IIFE，由视觉套件 globalSetup 现构建），逐条 path 比指令序列 + 数字容差 0.01px，并自检视口 / dpr 与夹具捕获时一致。夹具由 `pnpm gen:cross-impl` 刷新（需同机有 vue3 仓并先 build）                                                                                                                                                                                                        |
| G16  | ✅ 已收口 | `find *-chromium-linux.png` = 15 张（`d8aef8c`）；`visual.yml` 在 main 上连续 3 次 success                                                                                                                                                                                          |
| G17  | ✅ 已收口 | 跨仓**产物逐字 diff** 这条路实测走不通（两仓 `style.css` 只差头注释与 `@import` 路径、`transition.css` 全等，但压缩器会把同声明体的相邻规则合并、重排声明、把 `transparent` 写成 `0 0`、`.3s height` 写成 `height .3s`、颜色折成 `#fffffff0`），故按原建议的轻量做法钉几何值：两仓 `verify:dist` 的 CSS 段各加 **21** 条（六个几何变量的兜底值「处处存在且处处同值」12 条 + 左树短头三元组/`-1px`/`!important` 恰一次 5 条 + `okr-unstyled` 五类选择器 1 条 + `.is-measuring` 必须排在 `.is-measured` 之前等 2 条 + 补齐两端此前各缺一半的「无残留硬编码」1 条）。CSS 段两端现共 **33** 条、逐条同序同字面，唯一差别是消息里内插的实测数字（`--okr-line-width` 使用数 22 ↔ 21，正是 react 压缩器合并了两条同体 `:after` 规则）。五条变异在两仓各跑一遍、全部打红：兜底值 `20px→24px`、两条规则顺序颠倒、五类选择器降成四类、`calc(100% - 11px)→-12px`、`width:12px→14px`。探针 `_scratch/probe-css-mutation.mjs` / `probe-css-mutation-react.mjs` / `probe-css-gate-parity.mjs`                                                                                                                                                                                          |

第 7 节之外另有三项，一并校准：

- **「CI 各 job 没有一次历史 run 可查」→ ✅ 已消**。`ci.yml` 与 `visual.yml` 在 main 上均多次 success。`release.yml` 跑过两次：run `35579145408`（推 `v1.13.0`）publish 步被守卫标为 `skipped`、只补建 GitHub Release；**run `35711304130`（推 `v1.14.0`）publish 步 success，本包首次由 CI 经 OIDC 真实发包**，日志 `+ react-okr-tree@1.14.0` 与 `Provenance statement published to transparency log: …logIndex=2908890080`，`gh secret list` 为空即无 token 参与。
- **peer-matrix 字面不符 → 仍成立**。`ci.yml:112–115` 实际钉的是 react `~18.2.0` 与 `~18.3.0`，而 §7 与 requirements 483 行写的是「React 18.2 / 19.2」；`ci.yml:101–103` 的注释解释了 19 由默认 `verify` job 覆盖，但文档字面仍未改。
- **`wrangler deploy --dry-run` 无承载 → 仍成立**。`package.json`、`apps/website/package.json` 与全部 workflow 内 grep `wrangler` 零命中。

小结（同日第二批收口后）：G17 条里 **5 条已收口（G3 / G5 / G7 / G10 / G16）、1 条半收口（G11）、11 条仍成立**；表外 3 项里 1 项已消。**没有出现「声称做了、其实没实现」的功能级假 ✅** 这一条经复验仍然成立——剩余缺口全部是断言/门禁缺失，不是实现缺失。

**第 2 轮修复批次后的重新计数（2026-09-24）**：17 条 G 项 = **已收口 8**（G2 / G3 / G5 / G6 / G7 / G10 / G15 / G16）、**半收口 1**（G11）、**仍成立 8**（G1 / G4 / G8 / G9 / G12 / G13 / G14 / G17）。上面那行「5 条已收口」是当时批次的快照，保留不改。

**第 3 轮首批后的重新计数（2026-09-24）**：已收口 **11**（新增 G17 CSS 几何常量门禁、G4 警告族断言、G8 锁定态用例）、半收口 1（G11）、**仍成立 5**（G1 / G9 / G12 / G13 / G14）。

剩余优先级（按代价）：G1 是一行量级（但会动 `apps/website` 的 lockfile）；G13 需要新增视觉用例；G12 需要两条站内视觉用例。另有一条**新暴露的待验项**：`guide/controlled.mdx:91-93` 的「两种锁定强度」（内联字面量才完全锁定）尚未实测分离，见 G8 行末尾。（G2 / G15 / G17 / G4 / G8 / G9 / G14 已收口，见上表。）

**第 2 轮（1.14.0 外部审计修复批次）里的 G15 收口**：跨实现 DOM 比对已落成常驻门禁，形态是上表建议里的「夹具固化 vue3 侧结构串」那条（CI 拿不到 vue3 产物，故走人工刷新）。两条判据分别验过强度：改 `hiddenStyle` 的 `height: '0'` → `'2px'` 只打红 geometry（4 个含折叠容器的模式），改 `CLS.labelInner` 一个字母则 structure 与 geometry 一起红（9 个模式）——即第 1 轮报告说的「含内联样式逐字一致」在旧快照口径下并不成立，style 从来不在比对范围内，这一半现在才真的有人守。夹具刷新：`pnpm gen:cross-impl`（需同机有 vue3-okr-tree 仓且已 `pnpm build`）。

**同批补齐的 SVG d 值一半**：DOM 门禁对 `connector="svg"` 是明确让路的（SSR 只有空覆盖层，比不了），而 `svg-connector.ts` 恰好是两仓逐字同源、又最容易被几何常量漂移打穿的文件，所以补了 `cross-impl-svg.spec.ts`。五条变异逐个跑过，每条都是先红再还原（2026-09-24 实测，偏差值取自断言消息）：

| 变异（改 `src/svg-connector.ts`）                       | 打红的模式                                   | 偏差          |
| ------------------------------------------------------- | -------------------------------------------- | ------------- |
| curve 控制点封顶 `Math.min(40, …)` → `5`                 | `svg 连线 curve`、`curve（OKR 双向）`        | 15px          |
| 折线水平段 `l -20 0` → `l -14 0`                         | `curve（OKR 左树收起，含 stubPath）`         | 6px           |
| `stubPath` 侧向长度 `10` → `7`                           | 同上（`l 10 0` → `l 7 0`）                   | 3px           |
| `straight` 端点 `x2` → `x2 + 3`                          | `svg 连线 straight`                          | 3px           |
| `orthogonal` 竖向中点 `(y1+y2)/2` → `+4`                 | `svg 连线 orthogonal`                        | 4px           |

三个 shape 分支与 stub 各自至少被一条变异打红；第一条只打红 3 个 curve 模式里的 2 个，是因为第三个模式的 curve 段本就不受封顶值影响——不同模式之间不是冗余复制。第二、三条最初**抓不到**：第一版夹具里没有带 stub 的模式，补了「OKR 左树收起，含 stubPath」并重新捕获后才变红。这条门禁的两个自证式假绿风险也都排掉了：一是环境漂移——生成器最初的视口与套件实际视口不一致（`playwright.config.ts:37` 顶层写的是 1280×**800**，但 `projects[0].use = devices['Desktop Chrome']` 覆盖成 1280×**720**），这条自检当场把那个不一致判成失败而不是让它退化成像素巧合；之后生成器显式钉定 `viewport / deviceScaleFactor / locale / reducedMotion` 四项并全部写进夹具 `source`，两边各有来源、漂移必报；二是字体度量——svg 模式一律显式给 `labelWidth / labelHeight`（默认 `auto` 时盒子宽高由文字度量决定，换机器就换像素），定死之后 `d` 只由 style.css 的 gap / 线宽常量决定，Linux CI 才可比。

第二批收口后的门禁数字：单测 **259** 条（26 个文件，原 252），覆盖率维持 92.96 / 85.52 / 94.96 / 95.71，`verify:dist` 全过（含新的 UMD 真实执行 3 条与 CSS 六组动画逐个断言）。

**第 2 轮修复批次后（2026-09-24 复跑）**：单测 **278** 条（27 个文件；273 → 278 来自 3.3 的两条撑高窗口用例、3.4 的两条吞点击用例、以及 svg 稳态计数一条），视觉套件 **33** 条（原 14；+19 来自 G15 的两条跨实现门禁：`cross-impl.spec.ts` 12 条 + `cross-impl-svg.spec.ts` 6 条，另 1 条来自组对齐修复），`verify:dist` **45** 条 ok（2.6 / 2.7 换 Terser 那批补了三种产物各自的 ignore 注释断言；上面那行的 34 是当时批次数字，已过期），`pnpm typecheck` / `eslint` / `prettier --check .` 全净。上面那行 259 是当时批次的快照，不改。

**第 3 轮首批（G17 + G4 + G8，2026-09-24）**：`verify:dist` 45 → **66** 条 ok（CSS 段 +21，见 G17 行；上游 vue3 同批 41 → 62）；单测 278 → **281** 条（G4 一条 + G8 两条）。视觉 33 条不变、像素基线未动，**源码零改动**（三条都是测试/门禁/文档）。
